# 天枢深内核 · 架构文档

## 1. 整体流程
1. 上电 → 硬件加载 128 项查表 LUT
2. 内核启动，输出初始化指令序列
3. 内核从存储读取应用指令序列
4. 内核按序输出 7 位指令信号
5. 硬件查表执行
6. 支持跳转、循环、条件判断

## 2. 指令编码结构

  7 位指令 = [6 位自定义卦象][1 位阴阳极性]

  
- 6 位：000000 ~ 111111（共 64 种基础指令）
- 1 位：0（阴）/ 1（阳）
- 总指令：64 × 2 = **128 条**

## 3. 阴阳极性定义（参考）
- 阳（1）：正向、使能高、上升沿、读、发送
- 阴（0）：反向、使能低、下降沿、写、接收

## 4. 执行模型
- 单指令单动作
- 多指令组合 = 复杂逻辑
- 指令地址跳转 = 流程控制
- 无中断、无调度、无栈

## 5. 最小硬件要求
- 可存储 128 项映射表
- 可接收 7 位信号
- 可按查表输出硬件控制信号
- FPGA / 单片机均可实现
